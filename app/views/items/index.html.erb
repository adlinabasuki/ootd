<div class="container">
  <div class="filter-index mt-3">
    <p>Filter by:</p>
  </div>

  <div class="index-container row">
    <% @items.each do |item| %>
      <%= render "index_card", item: item %>
    <% end %>
  </div>
</div>
